@using SV22T1020161.Models.Sales
@model SV22T1020161.Models.Sales.OrderViewInfo
@{
    Layout = null;
}
<div class="modal-header text-bg-warning">
    <strong class="modal-title">
        <i class="bi bi-x-circle me-2"></i>Hủy đơn hàng #@Model.OrderID
    </strong>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>

<div class="modal-body">
    <div class="alert alert-warning d-flex align-items-center">
        <i class="bi bi-exclamation-triangle me-2"></i>
        Bạn có chắc chắn muốn hủy đơn hàng này không?
    </div>

    <dl class="row mb-0">
        <dt class="col-sm-4 text-muted">Khách hàng</dt>
        <dd class="col-sm-8">@Model.CustomerName</dd>

        <dt class="col-sm-4 text-muted">Ngày đặt</dt>
        <dd class="col-sm-8">@Model.OrderTime.ToString("dd/MM/yyyy HH:mm")</dd>

        <dt class="col-sm-4 text-muted">Tổng tiền</dt>
        <dd class="col-sm-8 fw-bold text-danger">@Model.TotalAmount.ToString("N0") ₫</dd>

        <dt class="col-sm-4 text-muted">Trạng thái hiện tại</dt>
        <dd class="col-sm-8">@Model.Status.GetDescription()</dd>
    </dl>
</div>

<div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
    <form method="post" action="~/Order/Cancel/@Model.OrderID" style="display:inline;">
        <input type="hidden" name="confirm" value="yes" />
        <button type="submit" class="btn btn-warning">
            <i class="bi bi-x me-1"></i> Hủy đơn hàng
        </button>
    </form>
</div>
